{
  "name": "splendit-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for SplenditSuite — exposes Firestore data to Claude",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "setup": "npm install && npm run build",
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "jest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "firebase-admin": "^13.7.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
