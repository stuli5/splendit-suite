# SplenditSuite — Pending Tasks

> Update this file as tasks are completed. Tell Claude: "pozri TODO.md a pokračuj"

## LinkedIn Chrome Extension

- [x] **Fix position/headline extraction** — MutationObserver + improved CSS selectors implemented
- [x] **Remove DEBUG block from Notes field** — done
- [x] **Update module** — implemented: lookup by LinkedIn URL, shows "Update CRM" mode if already in CRM, merges fresh LinkedIn data with existing CRM data
- [ ] **Repackage and publish extension** — zip rebuilt at `public/extension/splendit-linkedin-importer.zip` (v1.1.0), needs reload in chrome://extensions

## Custom Domain — splendidjob.cz


## Future / Backlog

- [ ] **Update Module enhancements** — show diff between existing and new data, highlight changed fields
